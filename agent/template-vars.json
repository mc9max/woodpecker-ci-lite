{
  "WOODPECKER_SERVER": {
    "type": "string",
    "description": "Server gRPC address — references the server service's private domain",
    "defaultValue": "${{woodpecker-server.RAILWAY_PRIVATE_DOMAIN}}:9000"
  },
  "WOODPECKER_AGENT_SECRET": {
    "type": "string",
    "description": "Shared secret for agent-to-server authentication — references the server's secret",
    "defaultValue": "${{woodpecker-server.WOODPECKER_AGENT_SECRET}}"
  },
  "WOODPECKER_GRPC_SECRET": {
    "type": "string",
    "description": "Shared gRPC secret — references the server's gRPC secret",
    "defaultValue": "${{woodpecker-server.WOODPECKER_GRPC_SECRET}}"
  },
  "WOODPECKER_MAX_WORKFLOWS": {
    "type": "string",
    "description": "Maximum number of parallel workflows",
    "defaultValue": "4"
  },
  "WOODPECKER_BACKEND": {
    "type": "string",
    "description": "Backend type (local, docker, kubernetes)",
    "defaultValue": "local"
  }
}
