{
  "woodpecker-server": {
    "private": "${{woodpecker-server.RAILWAY_PRIVATE_DOMAIN}}",
    "public": "${{woodpecker-server.RAILWAY_PUBLIC_DOMAIN}}"
  },
  "agent": {
    "private": "${{agent.RAILWAY_PRIVATE_DOMAIN}}",
    "public": "${{agent.RAILWAY_PUBLIC_DOMAIN}}"
  }
}
